import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Alfa Import & Export",
  description:
    "Alfa Import & Export - Leading supplier of industrial air compressors, Mercedes trucks, and electric motors for industrial applications across the Middle East region. Quality equipment, competitive prices.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html suppressHydrationWarning>
      <body className={inter.className}>{children}</body>
    </html>
  );
}
